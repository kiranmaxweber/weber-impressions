# Secrets — never committed. The repo goes public at submission and history is permanent.
.env

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Editor / tool config
.claude/

# Fonts: only the two faces the site uses are committed; the rest stay local for design work.
static/fonts/*
!static/fonts/OFL.txt
!static/fonts/Spectral-Regular.ttf
!static/fonts/Spectral-Bold.ttf
